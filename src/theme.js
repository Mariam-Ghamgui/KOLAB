import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffffff", 
    },
    secondary: {
      main: "#76B1A6", 
    },
    success: {
        main: "#EE7C92",
      },
  },
});

export default theme;